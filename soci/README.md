Example of using [bazel_soci](https://github.com/nathanws/bazel_soci).

### Building

From the project root, run:

```
bazel build //main:batcave
```

### Running

After building, from the project root run:

```
./bazel-bin/main/batcave -helpmatch main
```
